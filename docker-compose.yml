version: '3.8'

services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: symfony_php
    environment:
      - APP_ENV=prod
      - APP_DEBUG=0
      - JWT_PASSPHRASE=${JWT_PASSPHRASE}
      - APP_DOMAIN=${APP_DOMAIN:-localhost}
      - DATABASE_URL=postgresql://user:${DB_PASSWORD}@db:5432/app?serverVersion=16&charset=utf8
    volumes:
      - ./config/jwt:/var/www/config/jwt:ro
      - ./var:/var/www/var
    depends_on:
      - db
    networks:
      - app_network

  nginx:
    image: docker.io/library/nginx:alpine
    container_name: symfony_nginx
    ports:
      - "8080:80"
    volumes:
      - ./public:/var/www/public:ro
      - ./docker/nginx/conf.d:/etc/nginx/conf.d
    depends_on:
      - php
    networks:
      - app_network

  db:
    image: docker.io/library/postgres:16-alpine
    container_name: symfony_db
    environment:
      - POSTGRES_DB=app
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=${DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/db/initdb.d:/docker-entrypoint-initdb.d:ro
    networks:
      - app_network

networks:
  app_network:
    driver: bridge

volumes:
  postgres_data:
